apiVersion: v1
kind: Service
metadata:
  name: bd-frontend
  namespace: dev
  labels:
    app: bd-frontend
spec:
  type: ClusterIP
  selector:
    app: bd-frontend
  ports:
    - name: http
      port: 80
      targetPort: 3000
